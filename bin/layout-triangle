#!/usr/bin/env bash

set -eu

xrandr-smart \
	--dpi 96 \
	--output '!(eDP-*)#1' --mode 1920x1080 --pos 0x0 \
	--output '!(eDP-*)#0' --mode 1920x1080 --pos 1920x0 \
	--output 'eDP-*' --mode 1920x1080 --pos 960x1080 --primary

xset -dpms
