#!/usr/bin/env bash

set -eu

base="$(dirname "$(readlink -f "$0")")"
. "$base/.xlayoutlib"

fingerprint=$(fingerprint-outputs)
if [[ -f "$base/.xlayout/$fingerprint" ]]; then
	"$base/.xlayout/$fingerprint"
elif [[ "${1-}" ]]; then
	"$1"
else
	echo "unknown layout: $fingerprint"
fi
