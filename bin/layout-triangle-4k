#!/usr/bin/env bash

set -eu

if [[ ${1-} == "swap" ]]; then
	o1=0; o2=1
else
	o1=1; o2=0
fi

xrandr-smart \
	--dpi 96 \
	--output '!(eDP-*)#'$o1 --mode 3840x2160 --pos 0x0 \
	--output '!(eDP-*)#'$o2 --mode 3840x2160 --pos 3840x0 \
	--output 'eDP-*' --auto --pos 2880x2160 --primary

xset -dpms
