let g:lsp_autoformat_pylsp = v:true

if fnamemodify(expand('%'), ':~') ==# '~/bin/liskin-slack-status.py'
	lua vim.lsp.config('pylsp', { cmd = { vim.fs.abspath('~/bin/.venv-slack-sdk/bin/python'), '-m', 'pylsp' } })
	lua vim.lsp.config('pylsp', { settings = { pylsp = { plugins = { black = { enabled = true, line_length = 100 } } } } })
endif

if fnamemodify(expand('%'), ':~') ==# '~/bin/liskin-include-preproc.py'
	lua vim.lsp.config('pylsp', { settings = { pylsp = { plugins = { black = { enabled = true, line_length = 100 } } } } })
endif

if fnamemodify(expand('%'), ':~') ==# '~/bin/git-gh-stack'
	lua vim.lsp.config('pylsp', { settings = { pylsp = { plugins = { black = { enabled = true, line_length = 100 } } } } })
endif
